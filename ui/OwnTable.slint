
import { GridBox, HorizontalBox, VerticalBox } from "std-widgets.slint";
import { ProgramCheckBox } from "ProgramCheckBox.slint";
export component OwnTable {
    HorizontalBox {
        spacing: 60px;
        
        @children
    }
    
}